apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: product-be
  name: product-be
spec:
  replicas: 2
  selector:
    matchLabels:
      app: product-be
  template:
    metadata:
      labels:
        app: product-be
    spec:
      containers:
      - image: hashicorp/http-echo
        name: http-echo
        args:
        - "-text=Product Service Connected to DB"
        ports:
        - containerPort: 5678
        resources: 
          requests:
            memory: "250Mi"
            cpu: "250m"
          limits:
            memory: "500Mi"
            cpu: "500m"
